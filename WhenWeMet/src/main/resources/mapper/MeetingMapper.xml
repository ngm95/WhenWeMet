<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD MAPPER 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.spring.project.mappers.sql.MeetingMapper">
	<insert id="create">
        INSERT INTO meeting (
        mid
        , mname
        , creator
        ) VALUES (
        #{mid}
        , #{mname}
        , #{creator}
        )
    </insert>

	<select id="read" resultType="com.spring.project.dto.MeetingDTO">
		SELECT 
			mid,
			mname,
			creator
		FROM
			meeting
		WHERE 
			mid = #{mid}
	</select>
	
	<update id="update">
		UPDATE meeting
		SET
			mname = #{mname}
		WHERE
			mid = #{mid}
	</update>
	
	<delete id="delete">
		DELETE FROM meeting
		WHERE mid = #{mid}
	</delete>
	
	<select id="listAll" resultType="com.spring.project.dto.MeetingDTO">
		<![CDATA[
		SELECT 
			mid,
			mname,
			creator
		FROM
			meeting
		WHERE mid > 0
		ORDER BY mid DESC
		]]>
	</select>
	
	
</mapper>